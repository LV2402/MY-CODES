# 3️⃣ using a calculator (math.sqrt)

# your computer already knows how to do square roots.
# you can just ask it! but it gives decimals. so we cut off the decimal part.

import math

def floor_sqrt_builtin(n):
    return int(math.sqrt(n))
